1.variable_preparation code is for the preparation of the variables regarding land use(tree cover, human modification, adjusted
tree cover calculated as (1- human modification)*tree cover) and paleoclimate (Miocene and LGM climate anomaly);
see details in the R script and the result is stores in the data folder as dgg_tree_hm.rds

2.plotting code is for converting from dgg system(vector,sf object) to equal-area raster(see details in raster file);
visulization of all the variables in different years(period)

